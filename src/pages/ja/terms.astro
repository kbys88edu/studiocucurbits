---
import LegalDocument from '../../components/LegalDocument.astro';
import { legalDocuments } from '../../data/legal';
import BaseLayout from '../../layouts/BaseLayout.astro';

const document = legalDocuments.terms.ja;
---

<BaseLayout title={`${document.title} | Studio Cucurbits.`} description={document.description} canonicalPath="/ja/terms/" locale="ja">
  <LegalDocument slug="terms" locale="ja" />
</BaseLayout>
