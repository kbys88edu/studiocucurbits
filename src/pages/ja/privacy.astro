---
import LegalDocument from '../../components/LegalDocument.astro';
import { legalDocuments } from '../../data/legal';
import BaseLayout from '../../layouts/BaseLayout.astro';

const document = legalDocuments.privacy.ja;
---

<BaseLayout title={`${document.title} | Studio Cucurbits.`} description={document.description} canonicalPath="/ja/privacy/" locale="ja">
  <LegalDocument slug="privacy" locale="ja" />
</BaseLayout>
