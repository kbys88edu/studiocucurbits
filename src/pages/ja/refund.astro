---
import LegalDocument from '../../components/LegalDocument.astro';
import { legalDocuments } from '../../data/legal';
import BaseLayout from '../../layouts/BaseLayout.astro';

const document = legalDocuments.refund.ja;
---

<BaseLayout title={`${document.title} | Studio Cucurbits.`} description={document.description} canonicalPath="/ja/refund/" locale="ja">
  <LegalDocument slug="refund" locale="ja" />
</BaseLayout>
